[package]
name = "gairm-import"
version = "0.7.0"
entrypoint = "lib.typ"
authors = ["Shane Murphy"]
license = "MIT"
description = "JSON Schema → Typst dict coercer; ships with JSON Resume as the canonical example."
repository = "https://github.com/smur89/gairm-import"
keywords = ["json-resume", "resume", "cv", "loader", "schema"]
categories = ["utility"]
compiler = "0.15.0"
